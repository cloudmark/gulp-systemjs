"use strict";

var Readable = require("stream").Readable;
var assert = require("assert");
var gutil = require("gulp-util");
var systemJsFilter = require("./index");


describe("gulp-systemjs", function() {
  it("should reduce systemjs calls", function (done) {
    done();
  });
});
